# 2D地図・スターバックス参加型情報の公開

対象は個人用XServerのDeaf Naviのみ。前回の「本番公開OK」に続く同コンテンツの改修依頼に基づく。KAGOYA、DNS、Caddy設定、PHPサービス、アカウント、メール設定は変更しない。実メール送信・本番テスト投稿は行わない。

## 変更範囲

- 地図URLは維持し、初回操作後のみLeaflet 1.9.4 / markercluster 1.5.3を読み込む2D地図に切り替える。検索・地域絞り込み・パン・ズーム・対応ブラウザの全画面・HTML店舗一覧を用意。
- ライブラリは自己ホスト。OSMの表示範囲タイルだけ外部取得。地図ページ限定CSPでOSM画像とLeaflet透明画像用data:を許可。管理画面のCSPは変更しない。
- カードリンクの矢印文字をSVGの意味別アイコン部品へ変更。
- 店舗・日時・条件・任意情報源の短い投稿フォーム。CSRF、既存の時間制限/頻度制限、500文字のサーバー検証、承認待ち・通知待ち保存。氏名・メール不要。
- 町田パリオ、町田東急ツインズEAST、敷島公園の3店舗と3開催実績を追加。常設カフェ一覧には混在させない。日時不詳の実績にEvent構造化データを出さない。

情報源は content/connect/starbucks-observed-20260905.json に記録。町田の2025-11-06は報告会の日であり開催日には使わない。敷島公園も周辺の別企画の日付を流用しない。新規DB用seedと、既存DB向けの明示CLI追加インポートを分離。

## 検証

- npm test 63件、既存PHP HTTPテスト73件、PHP全ファイル構文検査。
- test/starbucks.integration.mjs: 既存23行・利用者・設定・投稿を保持した6件追加、重複実行拒否、整合バックアップ、不正入力8種、正常投稿2件が確認待ち/通知待ち、公開一覧非表示。
- ブラウザ1280/768/375px: 検索、地域、一覧選択、ポップアップ、ズーム、パン、表示範囲、フォーム切替、文字数、ページ横溢れなし、console/pageエラーなし。JS無効とAPI失敗時は18件のHTMLリンクを保持。
- 自動ブラウザではOSM公開タイルへアクセスしないよう画像を模擬。実背景地図は公開後に可視ブラウザで別途確認する。

## 公開前状態・復旧

公開前: df7639338d20458414925381384c8dd245911ce8-33965840964-1。
バックアップ予定: /srv/deafnavi/shared/directory/backups/map2d-20260905。
GitHub Actionsでリリースを作成しcurrentを原子的に切り替える。独立したCLIで6件だけ追加し既存行を変更しない。PHPキャッシュはDeaf Naviバックエンドだけ無効化し、共有サービスを再起動しない。

復旧は保存した旧releaseへcurrentを原子的に戻し、同じアプリ限定キャッシュ処理を行う。追加データは既存行を上書きしていないため、必要なら新規6IDを管理画面で非公開にする。DB全体の復元は新しいユーザー投稿を失うため自動では実行しない。

公開結果・本番検証はタスクの公開記録に追記する。ここまでの記述だけでは公開完了を意味しない。
